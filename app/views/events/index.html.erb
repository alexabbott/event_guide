<div class="main_content">

	

	<%= render :partial => 'carousel.html.erb' %>

<!-- 	<h1 class="more_events_header">More Free Events</h1><br><br>
 -->
 	<br>
	<%= render :partial => 'filter_nav.html.erb' %>


	<% @events.each do |e| %>
		<div class="col-md-3 col-sm-6 events_container">
			<a href="/events/<%= e.id %>" class="event_link" style="text-decoration: none; color:black;" name="event_anchor"><div class="each_event">
				<h3><%= e.title %></h3><br>
				<span class="at_symbol">@</span> <%= e.venue %></p>
				<p><%= e.date.strftime("%A, %b %d") %> <span class="at_symbol">|</span> <%= e.time.strftime("%I:%M %p") %>
			</div>
			<img src="<%= e.imageurl %>" class="index_event_image"></a>
		</div>
	<% end %>



	<%= render :partial => 'event_modal.html.erb' %>

	<%= render :partial => 'signup_modal.html.erb' %>

	<%= render :partial => 'login_modal.html.erb' %>

</div>

<div class="footer">
			<div class="footer_text">
				<img src="../../assets/freela.png" class="footer_logo"><br><br>
				<strong>Free::LA</strong> is the definitive guide to free concerts, parties, festivals and more awesome events in Los Angeles.<br>
			<a href="" data-toggle="modal" data-target="#signupModal">Sign up</a> now to add your own events.</div>
</div>
